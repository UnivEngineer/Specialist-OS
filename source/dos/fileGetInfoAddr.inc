;+---------------------------------------------------------------------------
; MXOS
; Получить указатель на информацию о a==1
;
; На входе 
;   c - любое число
;
; На выходе 
;   b, de - сохраняются
;   hl - указатель на дескриптор после расширения
;   zf - если a==1
;   a = c
;   c = c-1
;
; 2013-12-12 Дизассемблировано vinxru
;----------------------------------------------------------------------------

;fileGetInfoAddr:
;            push de
;
;            ; Помещаем в hl адрес информации о файле
;            ld    hl, (v_foundDescrPtr)
;            ld    de, FILE_DESCRIPTOR.attrib
;            add   scf
;
;            ; Помещаем в a режим
;            ld    a, c
;
;            ; ZF=1 если режим 1
;            dec c
;
;            pop de
;            ret
;
