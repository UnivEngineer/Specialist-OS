;----------------------------------------------------------------------------
; MXOS NC.COM
; Вывод инверсной строки
;
; 2013-12-18 Дизассемблировано vinxru
;----------------------------------------------------------------------------

printStringInv:   ld    a, 1
            ld    (bios_vars.inverse),a
            call  bios_printString
            xor   a
            ld    (bios_vars.inverse),a
            ret

