;+---------------------------------------------------------------------------
; MXOS
; Вызывается при ошибке чтения с ленты
;
; 2013-12-12 Дизассемблировано vinxru
;----------------------------------------------------------------------------

tapeReadError:    pop   de
            pop   bc
            ld    hl, (bios_vars.tapeError)
            jp    (hl)

