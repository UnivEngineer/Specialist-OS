# TODO

## Документация ABI

- [ ] Создать каталог `docs/`.
- [ ] Экспортировать листы Excel с точками входа ABI в UTF-8 CSV.
- [ ] Перенести документацию в `docs/ABI.md` и дальше вести её в репозитории.
- [ ] Для каждой точки входа указывать:
  - адрес;
  - имя;
  - компонент;
  - входные и выходные регистры;
  - разрушаемые регистры и флаги;
  - семантику CF и коды ошибок;
  - совместимость и примечания.
- [ ] Добавить PowerShell-скрипт преобразования CSV в Markdown, если ручное ведение
  большой таблицы окажется неудобным.
- [ ] Добавить проверку соответствия адресов в `docs/ABI.md` и
  `source/include/mxos.inc`.

## Подготовка реорганизации репозитория

- [ ] Сначала зафиксировать текущее рабочее состояние отдельным коммитом и,
  желательно, тегом.
- [ ] Составить список всех отслеживаемых файлов и разделить их на:
  - исходники;
  - незаменимые бинарные входные данные;
  - генерируемые артефакты;
  - пользовательские программы и игры;
  - legacy-файлы Visual Studio.
- [ ] Собрать проект до реорганизации и сохранить SHA-256 всех основных
  результатов: `DOS.SYS`, системных программ, ROM- и flash-образов.
- [ ] Проверить, что каждый файл, который планируется исключить из Git, можно
  воспроизвести из исходников либо получить из документированного внешнего
  источника.

## Целевая структура

```text
source/
  include/
  dos/
  nc/
  E/
  flash/
  format/
  launch/
  mon2/
  tape/
  test-scr/

scripts/
  build/
  images/
  emulator/
  listings/

docs/
  ABI.md
  memory-map.md

assets/
  games/
  rom-input/
  fonts/

bin/                    # генерируемые программы, в .gitignore
images/                 # генерируемые ROM/flash-образы, в .gitignore
dist/                   # только специально опубликованные релизы
```

- [x] Перенести `include/` в `source/include/`. При одновременном переносе
  компонентов в `source/*` большинство путей `../include/mxos.inc` останутся
  прежними.
- [x] Перенести каталоги исходников DOS, NC и системных программ в
  `source/<component>/`.
- [ ] Не смешивать незаменимые бинарные входные данные из `assets/` с
  генерируемыми файлами в `bin/` и `images/`.
- [ ] Решить, должны ли опубликованные эталонные образы храниться в `dist/`,
  Git LFS или только во внешних релизах.

## Перенос файлов с сохранением истории

- [ ] Первым отдельным коммитом выполнить только перемещения файлов, не меняя
  их содержимое.
- [ ] Использовать `git mv` либо после перемещения проверить распознавание
  переименований командой `git diff --summary -M`.
- [ ] Вторым отдельным коммитом исправить:
  - пути `INCLUDE` и `INCBIN`;
  - `Specialist-OS.code-workspace`;
  - задачи сборки;
  - пути к входным и выходным файлам;
  - пути в `AGENTS.md`, `README.md` и документации;
  - подготовку листингов и запуск эмулятора.
- [ ] Проверить историю перемещённых исходников через `git log --follow`.
- [ ] Не применять `git filter-repo`, если нет отдельной задачи удалить большие
  бинарники из всей истории. Обычный перенос и удаление файлов историю не
  уничтожают.

## Миграция сборочных скриптов на PowerShell

- [ ] Перенести общую сборку компонентов из legacy BAT-обёрток в PowerShell.
- [ ] Переписать `ROM/makeRom/-makeRom.js` на PowerShell.
- [ ] Переписать сборщики flash-образов на PowerShell.
- [ ] Использовать явные манифесты входных файлов вместо неявного включения
  всего содержимого каталога.
- [ ] Сохранить поддержку:
  - эмуляторного системного ROM;
  - сырых аппаратных образов 128, 256 и 512 КБ;
  - разных `AUTOEXEC.BAT` для эмулятора и физической машины;
  - наборов flash-диска `Games64k`, `Games` и `Games2M`;
  - верхнего регистра системных имён и исходного регистра пользовательских
    файлов.
- [ ] На переходном этапе сравнивать старые и новые образы побайтно либо
  разбирать FAT-каталоги и сравнивать содержимое файлов.
- [ ] Удалять JS/BAT-скрипт только после подтверждения эквивалентности
  PowerShell-версии.

## Генерируемые файлы и `.gitignore`

- [ ] Направить `*.com`, `*.sys`, `*.exe`, `*.lst`, `*.fix.lst`, `list.tmp` и
  образы накопителей в выделенные выходные каталоги.
- [ ] Добавить `bin/`, `images/` и временные каталоги сборки в `.gitignore`.
- [ ] Помнить, что `.gitignore` не прекращает отслеживание уже добавленных
  файлов. После проверки воспроизводимости удалить генерируемые файлы только
  из индекса Git.
- [ ] Не удалять массово игры, эталонные образы и другие бинарные входные данные
  до определения их происхождения и назначения.
- [ ] Для крупных, но необходимых бинарных входных данных рассмотреть Git LFS
  или отдельный архив/репозиторий.

## Удаление legacy Visual Studio

- [ ] Найти устаревшие `.sln`, `.vcxproj`, `.vcxproj.filters`, `.user`, `.vs`,
  `Release/*.tlog` и другие файлы старой сборки Visual Studio.
- [ ] Проверить, что они не используются текущими задачами VS Code и
  PowerShell-скриптами.
- [ ] Удалить legacy-файлы отдельным коммитом.
- [ ] Сохранить `Specialist-OS.code-workspace` и актуальные настройки VS Code.

## Проверка после реорганизации

- [ ] Выполнить `build: all`.
- [ ] Собрать системный ROM для эмулятора.
- [ ] Собрать сырые аппаратные ROM-образы 128, 256 и 512 КБ.
- [ ] Собрать все варианты flash-образов.
- [ ] Сравнить итоговые файлы с сохранёнными контрольными суммами.
- [ ] Проверить BPB, FAT, корневой каталог, имена и содержимое файлов в образах.
- [ ] Запустить эмулятор и проверить:
  - загрузку DOS и NC;
  - установку ROM.COM на H:;
  - RAM-диск B:;
  - запуск системных утилит;
  - копирование, переименование и удаление файлов.
- [ ] Отдельно перечислить то, что всё ещё требует проверки на физической
  машине: flash-запись, страничное переключение, теневое ПЗУ, магнитофон и
  тайминги ВИ53.

## План развития кода

### Уже реализованная основа

- [x] Отказаться от добавления полноценных `fseek`/`fread`/`fwrite`, пока нет
  прикладных программ, которым они действительно необходимы.
- [x] Использовать при копировании файла 32-КБ область пользовательской памяти
  `0000h–7FFFh` как временный буфер.
- [x] Выделить из `fileLoad`/`fileLoad2` общий цикл передачи кластеров с
  поддержкой чтения и записи.
- [x] Реализовать блочное копирование файлов размером больше доступного
  32-КБ буфера.
- [x] Проверить копирование файла размером 51118 байт на фрагментированный
  RAM-диск и сравнить результат с источником.

### Ближайшие задачи

- [ ] Реализовать прогресс-бар копирования в NC.COM.
  - [ ] Не выводить его для файлов меньше 1 КБ.
  - [ ] Выбрать компактный способ обновления: после каждого килобайта либо по
    завершении очередного блока копирования.
  - [ ] Не добавлять функцию оценки времени драйвера, пока простой индикатор
    объёма даёт достаточную обратную связь.
  - [ ] Учесть ограничение текущей области NC.COM и оценить прирост бинарника
    по листингу.
- [ ] Исследовать возможность уменьшения шрифта.
  - [ ] Получить фактический набор символов, используемых DOS, NC и системными
    утилитами.
  - [ ] Отдельно учесть русский KOI-8R, управляющие символы и символы,
    необходимые пользовательским программам.
  - [ ] Не удалять символы только на основании отсутствия псевдографики в
    NC.COM.
- [ ] Исследовать перенос NC.COM из системной области E800h–FAFFh в
  пользовательскую память с адреса 0000h.
  - [ ] Определить диапазон NC.COM, его стека, списка файлов и временных
    буферов.
  - [ ] Устранить пересечение кода NC.COM с 32-КБ буфером копирования.
  - [ ] Определить, какие области пользовательской памяти NC.COM может
    сохранять или восстанавливать при запуске внешних программ.
  - [ ] Сохранить возможность запуска MON2.COM для работы с произвольными
    блоками пользовательской памяти.
  - [ ] Оценить, какую часть E800h–FAFFh удастся освободить для DOS, драйверов,
    дискового кэша и буферов путей.
- [ ] Исследовать выделение графического движка NC.COM в системный резидентный
  модуль.
  - [ ] Определить минимальный ABI рисования окон, таблиц, кнопок и строк
    ввода.
  - [ ] Оценить размер резидентного кода и таблицы переходов.
  - [ ] Проверить возможность повторного использования движка редактором,
    TAPE.COM и другими системными утилитами.
  - [ ] Не связывать прикладные программы прямыми переходами во внутренние
    метки NC.COM.
- [ ] Спроектировать системное окно выбора файла.
  - [ ] Определить общий формат результата: диск, путь, имя, расширение и
    действие пользователя.
  - [ ] Поддержать вызов из произвольной программы через стабильную точку
    входа.
  - [ ] Предусмотреть отмену без изменения состояния вызывающей программы.
  - [ ] Сопоставить интерфейс с точкой Ramfos `F824h` `dlg_loadFile`.

### Файловая система и каталоги

- [ ] Завершить приведение текущей FAT16-подобной файловой системы к
  каноническому FAT16.
  - [ ] Исправить BPB и служебные значения FAT16.
  - [ ] Реализовать стандартную семантику корневого каталога.
  - [ ] Исправить обработку размера файла, включая пустые файлы и границы
    сектора/кластера.
  - [ ] Сохранить расширение MXOS с адресом загрузки исполняемого файла и
    документировать используемые байты дескриптора.
  - [ ] Сохранить поддержку физических секторов по 256 байт.
- [ ] После завершения FAT16 отдельно спроектировать FAT12.
  - [ ] Не добавлять FAT12 в текущий код без отдельной задачи и версии формата.
  - [ ] Реализовать чтение и запись чётных и нечётных 12-битных элементов без
    повреждения соседних четырёх битов.
  - [ ] Учесть дискеты с логическими секторами по 512 байт.
- [ ] Реализовать поддержку подкаталогов.
  - [ ] Выбрать максимальную длину полного пути: 128 или 256 символов.
  - [ ] Отделить обход каталога от физического драйвера носителя.
  - [ ] Реализовать переходы `.` и `..`, поиск свободного элемента и работу с
    цепочками каталогов.
  - [ ] Добавить защиту от повреждённых и циклических цепочек.
- [ ] Реализовать MkDir в DOS.SYS.
- [ ] Подключить MkDir к зарезервированной кнопке `F7 MkDr` в NC.COM.

### Драйверы накопителей

- [ ] Реализовать драйвер флоппи-дисковода.
  - [ ] Спроектировать или выбрать аппаратный контроллер.
  - [ ] Поддержать логические сектора 512 байт при существующем 256-байтном
    интерфейсе физических блоков.
  - [ ] Проверить чтение, запись, форматирование, границы дорожек и обработку
    ошибок.
- [ ] Спроектировать одновременное размещение двух внешних драйверов из набора
  floppy/flash/tape/flashsys.
  - [ ] Проверить существующие области по 512 байт.
  - [ ] Решить, нужны ли два варианта каждого драйвера для разных адресов или
    общий перемещаемый формат.
  - [ ] Сохранить встроенный RAM-драйвер либо отдельно оценить выигрыш от его
    выноса из DOS.SYS.
  - [ ] Реализовать установку драйвера на выбранную букву диска.

### Совместимость с Ramfos

- [ ] Спроектировать отдельное совместимое ядро или адаптер с таблицами
  переходов Ramfos по адресам C800h и F800h.
- [ ] Не смешивать ядра прямыми переходами: загружать совместимое ядро так же,
  как сейчас загружается Monitor-2 для запуска RKS.
- [ ] Реализовать точку `F824h` `dlg_loadFile` с файловой таблицей.
- [ ] Использовать общий FAT-формат DOS.SYS вместо собственного формата
  RAM-диска Ramfos.
- [ ] Разделить:
  - таблицу ABI и стандартные подпрограммы Ramfos;
  - файловую систему и драйверы накопителей MXOS.
- [ ] Определить состояние, которое требуется сохранять при переключении между
  MXOS, Monitor-2 и совместимым ядром Ramfos.
- [ ] Проверить программы Ramfos, использующие системную форму выбора файла,
  дисковые функции и стандартные подпрограммы.
