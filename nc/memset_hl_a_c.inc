;----------------------------------------------------------------------------
; MXOS NC.COM
; Заполнение памяти по адресу hl байтом a длинной c
;
; 2013-12-18 Дизассемблировано vinxru
;----------------------------------------------------------------------------

memset_hl_a_c:
        ld      (hl), a
        inc     hl
        dec     c
        jp nz,  memset_hl_a_c
        ret

